<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Venta / Servicio</title>

    <link rel="stylesheet" href="/css/styleClients.css">
    <link rel="stylesheet" href="/css/styleSales.css">
    <link rel="stylesheet" href="/css/styleNav.css">
</head>
<body>

<nav>
    <div class="logo">SaleySpa</div>
    <button id="burger" class="hamburger" aria-label="Menú">
        <span></span><span></span><span></span>
    </button>

    <aside id="nav-menu" class="nav-menu">
        <a href="/">Inicio</a>
        <a href="/clients">Clientes</a>
        <a href="/products">Productos</a>
        <a href="/sales">Ventas</a>
        <a href="/logout">Salir</a>
    </aside>
</nav>

<div class="container">
    <h1>Editar Venta / Servicio</h1>
    <% if (error) { %><p class="error"><%= error %></p><% } %>

    <!-- (el formulario existente se mantiene igual) -->
    <form class="form-client" method="post" action="/sales/<%= sale.id %>/edit">
        <!-- … todo tu formulario sin cambios … -->
        <!-- código omitido por brevedad, es igual al que ya funciona -->
    </form>

    <p class="link"><a href="/sales">← Volver al listado</a></p>
</div>

<!-- scripts -->
<script>
    // menú
    const b=document.getElementById('burger'),m=document.getElementById('nav-menu');
    b.onclick=()=>{b.classList.toggle('active');m.classList.toggle('active');};
    document.addEventListener('click',e=>{
        if(!m.contains(e.target)&&!b.contains(e.target)){b.classList.remove('active');m.classList.remove('active');}
    });
</script>

<!-- tu script de sincronizar precio y origen (ya lo tenías) -->
<script src="/js/sales-edit.js"></script><!-- si lo externalizas -->
</body>
</html>
